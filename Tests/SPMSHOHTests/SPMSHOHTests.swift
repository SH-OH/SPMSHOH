import XCTest
@testable import SPMSHOH

final class SPMSHOHTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SPMSHOH().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
